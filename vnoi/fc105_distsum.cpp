#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main() {
    int n;
    cin >> n;
    ll x[n], y[n];
    for (int i = 0; i < n; i++) cin >> x[i] >> y[i];

    sort(x, x + n);
    sort(y, y + n);

    for (int i = 0; i < n; i++) {
        
    }

    return 0;
}