#include <bits/stdc++.h>
using namespace std;

int main()
{
    int x, a, y, b, m;

    freopen("BRIDGE.INP", "r", stdin);
    freopen("BRIDGE.OUT", "w", stdout);
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    cin >> x >> a >> y >> b >> m;

    int ans = 0;
    int i = 0;
    
    return 0;
}