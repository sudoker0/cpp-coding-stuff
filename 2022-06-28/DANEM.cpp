#include <bits/stdc++.h>
using namespace std;

int main()
{
    double N;
    freopen("DANEM.INP", "r", stdin);
    freopen("DANEM.OUT", "w", stdout);
    cin >> N;
    cout << ceil(N / 9);
}
